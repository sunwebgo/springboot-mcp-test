server:
  port: 8081

spring:
  application:
    name: mcp-test
  ai:
    mcp:
      server:
        name: test-mcp-server
        version: 1.0.0
        protocol: STREAMABLE
        streamable-http:
          mcp-endpoint: /mcp
